# 项目推进清单

这份清单用来固定后续节奏：用户侧申请、付款、才虫投稿等事情在等待时，开发侧继续推进不依赖外部结果的工作。

## 当前已完成

- 真实才虫 API Key 已接入，当前不是模拟模式。
- Supabase 已接入，订单、用户、附件、投稿、操作日志已能保存。
- 手机号开发登录已可用，生产环境需接真实短信服务。
- 真实 1 元测试单已创建并支付。
- 才虫投稿已同步到本地，投稿内容可读取。
- 心跳同步支持手动触发和定时触发。
- 订单短信提醒链路已接入：收到投稿、进入选择期、选择截止前 6 小时均有模板和幂等日志。
- 本地 `/admin` 已改为后台专用手机号登录入口，当前本地管理员手机号为 `18201500661`。
- 后台 `/admin` 已有上线健康检查、订单概览、短信提醒记录、最近日志。
- 首页已接入市场动态预览：展示发单统计、任务用例、分类筛选和需求详情抽屉。
- 旧测试单已从真实运营统计中排除。

## 需要用户亲自处理

- 生产环境继续维护真实短信服务模板和阿里云模板 Code。
- 确认正式域名、站点名称、Logo、基础文案。
- 上线前在 Vercel 配置生产环境变量。
- 真实选择投稿前，确认是否允许触发才虫结算。

## 我可以继续做的工作

1. 完成选择投稿后的真实闭环校验
   - 服务端已增加投稿归属校验。
   - 前端已增加结算确认提示。
   - 等用户确认后，再对真实 1 元测试单执行一次选择投稿。

2. 完善运营后台
   - 后台登录页已可用，后续可补后台登出入口和更细的管理员会话提示。
   - 短信提醒记录已可查看最近 30 条，含提醒类型、用户手机号、发送状态、发送时间、关联订单和失败原因。
   - 增加订单详情入口。
   - 增加按状态筛选。
   - 增加异常日志的可读说明。
   - 增加“需要处理”的订单提示。

3. 完善普通用户体验
   - 优化订单状态文案。
   - 明确支付后、等待投稿、选择投稿、已完成的下一步提示。
   - 优化空状态和错误提示。

4. 上线准备
   - 核对 `.env.example`。
   - 核对订单提醒短信模板 Code。
   - 核对市场动态统计表、基数和才虫市场数据 API Key。
   - 配置外部 HTTP Cron 作为 5 分钟订单提醒主调度。
   - 保留 GitHub Actions 的 30 分钟提醒心跳作为兜底。
   - 完成部署前检查文档。
   - 列出生产环境必须关闭的开发能力。

5. 安全和权限
   - 继续检查所有订单接口是否只允许订单本人访问。
   - 检查管理员入口是否只允许 `ADMIN_PHONES`。
   - 确保 service role 和才虫 API Key 只在服务端使用。

## 暂时不做

- 独立用户 Agent 模式：第一版先走平台代理 Agent。
- 正式视觉品牌大改：先把业务闭环做稳。
